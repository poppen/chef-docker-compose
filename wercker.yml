box: masutaka/rvm-vagrant-aws@1.2.0
build:
  steps:
    - bundle-install:
        jobs: 4
        without: development
    - script:
        name: kitchen test
        code: |
          bundle exec kitchen test
  after-steps:
    # Send notification
    - wantedly/pretty-slack-notify:
        webhook_url: $SLACK_WEBHOOK_URL
        channel: general
